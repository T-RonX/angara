# Gemini CLI Configuration

---
## Project Overview

**Project name:** Angara (working title)  
**Description:** A web based always online multiplayer resource management game where the player collects resources for the greater good. It features a scrollable map with a grid that hides resources which can be mined or harvested. It does not feature combat, instead it is based on working together but no without its challenges.

---

## Technical details

**Backend Framework:** Symfony 7.4; PHP 8.4 (I am a Symfony veteran, just get to the point.)  
**Frontend Framework:** Vue 3; TypeScript (I am new to it, help we explain to teach me.)  
**Database:** MySQL 8.4  
**OS**: Ubuntu
**Web server**: nginx

### Principes

- The project is supposed to become a single-page application (SPA), but served from the Symfony backend.
- Doctrine migrations are not yet used. Changes can be made directly, just use `doctrine:schema:update --force`.
- Tests are not pervasive. There is a functional test suite for the economy tick (see **Testing** below); keep it green and extend it when touching that area. Do not add tests elsewhere unless explicitly asked.

---

## Keeping this file updated

- **Always update this AGENTS.md when you learn something relevant**: a new workflow, an environment gotcha, an architectural decision, a convention, or a command worth remembering. Treat it as the living source of truth for working in this repo so future sessions don't rediscover the same things.

---

## Repository Structure

### Backend:
This is just your typical Symfony project.

`config/`: Contains the typical YAML config for a Symfony project.  
`docker/`: All the Docker containers the project uses (`app` (PHP), `db` (MySQL) and `web` (nginx)).  
`public/`: Entry point for the application.  
`src/`: Contains the usual source code for a typical Symfony structure.  
`templates/`: Contains the usual Twig templates for a typical Symfony structure.  

## Architecture
- Always use transactions for database operations at the highest level like controllers or commands or facades.
- Use fixtures for seeding the database. New seeds should be added as new fixtures.
- In the inline Three.js body explorer (`templates/index/index.html.twig`), the resource-mode clip plane always passes through the body centre, so the cliff exposes the full depth stack (every shell + the core) in both traversal modes.
- In that same inline Three.js renderer, hover/selection highlight materials should keep `depthTest: true` (with polygon offset) to avoid transparent triangle artifacts and missing overlay patches near clipped seams.
- Resource-mode traversal direction is configurable via `CONFIG.resourceTraverseAxis` (`'longitude' | 'latitude'` in `templates/index/index.html.twig`); the clip-plane normal, the cap broad-phase, drag/arrow input mappings, and the camera all branch on it.
  - `'longitude'`: the cut is a meridian (constant-longitude) plane through the poles. You travel around the equator and the poles sit to the left/right. Only a longitude change rebuilds the caps (latitude just pans the camera along the cliff).
  - `'latitude'`: the cut is a through-centre plane whose normal is the north tangent at the focus, so tilting it (changing latitude) sweeps the cut pole-ward. You travel toward the poles and they sit top/bottom. Both axes move the plane, so either change rebuilds the caps.
- The resource camera always looks edge-on along the clip-plane normal with `camera.up` on the surface radial, so depth stays vertical on screen in both modes; only the lateral orientation of the poles changes between axes.
- In `'latitude'` mode the tilted great-circle cut stays within +/- the focus latitude and never reaches the caps while you are between the poles, so cap cells are skipped from that cross-section (`greatCirclePhase` in `buildCaps`). However, the focus latitude can now stroll **onto** a polar cap: `latStops` adds both cap centres to the discrete latitude stops, and when the focus enters the cap band `isPoleCut()` flips the cut to a vertical **meridian** plane (via `updateCut`) so the existing meridian `capCellCrossSection` exposes the pole's full depth/layer stack. Drag/arrow input clamps to `capCenterLatN/S` so the poles are reachable and exitable one stop at a time (`meridianPhase` includes the caps in `buildCaps`).
- The atmosphere can be a **selectable cell shell** (`CONFIG.atmosphereSelectable`, single flag for now — per-body data comes later from the backend). `atmosphereCells` are built on the SAME lon/lat grid (quads + caps) between `planetRadius` and `atmosphereRadius`; the visible haze is still the scattering shader. They are emitted into `buildCaps` as a near-invisible cross-section strip (`atmosphereCapMaterial`, added to `capMeshes` for picking) and are pickable **only in resource mode** (globe-mode `pickSurfaceCellAnalytical` stays surface-only). Their resource-mode hover/selection highlight is a **wireframe box** (full hex, fill hidden) instead of a filled cross-section. The atmosphere **counts as a layer** in the depth readout (`layerLabel`): it's the OUTERMOST layer (layer 1), so the crust layers shift down by one and the total grows by one when the shell exists.
- Lighting: the `sunIntensity` slider drives **only** the direct `sun` light (the lit side). The dark-side fill (`ambient` + `rim` + `hemiFill` + `bottomFill` + the atmosphere glow) is decoupled from it and held at a constant floor scaled by `CONFIG.nightDarkness` (0 = pitch-black night side, 1 = full configured fill) so the night side never tracks the sun. Both are wired to HUD sliders.
- Entering/leaving resource mode is a smooth, configurable fly-through (`CONFIG.modeTransitionMs` in `templates/index/index.html.twig`): the camera eases between the orbit pose and the crust pose (`computeCrustPose()`) while the clip plane sweeps `constant` from `planetRadius` (whole body) to `0` (open slice). It is driven by `stepTransition()`/`finishTransition()` from the `animate()` loop via `state.transition` (`active`/`dir`/`s`/`orbit`). A mid-flight mode-button click just flips `dir` to glide back; all gameplay input is locked while `transition.active`. During the sweep the plane is off-centre, so `buildCaps(slab=true)`/`updateCut(constant, slab)` use a generalised bounding-sphere broad-phase (`cellBounds`) instead of the through-centre column/row phases; polar caps (non-offset-aware `capCellCrossSection`) are skipped mid-sweep and reappear when the cut settles via the final `updateCut(0)`.

## Fixtures and migrations
- Always make sure the foreign keys are the first columns after the primary key or logical order. 

### Frontend:
`assets/`: Root directory for the frontend project.
`assets/src/Rendeder`: Contains a custom render engine based on Canvas 2D. It must remain game agnostic.
`assets/src/Game`: Contains the main game code where not directly part of Vue.
`assets/src/components/Rendering/RenderViewport.vue`: Is the entry point of the application where the Game class is initialized along with some crucial parameters.
`templates/index/index.html.twig`: Current runtime page for `/` (rendered by `IndexController`). It now only holds the HUD markup + styles + the `three` importmap and loads the renderer via `<script type="module" src="/explorer/index.js">`.
`public/explorer/`: The Three.js body/resource renderer, refactored out of the old inline script into small **single-responsibility plain-JS ES modules** (no build step; served statically from `public/`). Data is split from implementation: all tunables live in `public/explorer/config/physical.js` (planet, layers, atmosphere, suns, starfield, lighting) and `config/behaviour.js` (camera, input, traversal, transition) — eventually backend-sourced. `BodyExplorer.js` is the orchestrator (owns `state`, mode switching, the animate loop) and wires the subsystems under `core/`, `model/`, `material/`, `geometry/`, `world/`, `lighting/`, `star/`, `sky/`, `atmosphere/`, `slicing/`, `picking/`, `navigation/`, `transition/`, `hud/`. The old `CONFIG.*` knobs map onto these config objects; behaviour is otherwise unchanged.
  - Mouse drag sensitivity, drag direction sense, and wheel zoom response belong in `config/behaviour.js` and are consumed by the navigation helpers (`navigation/InputController.js`, `navigation/CrustCamera.js`, and the topology traversal classes). Keep new mouse-feel knobs there instead of hardcoding them in traversal code.
  - **Multiple suns**: each light source is a self-contained `star/Star` (its own directional light + sun disc + chromatic halo + starburst + lens flare + occlusion) held in `star/StarSystem`. `atmosphere/AtmosphereShell` sums scattering over `NUM_SUNS` (compiled from the config star count), and `hud/SliderPanel` generates one az/el/intensity control group per sun, so changing `physical.stars[]` rescales lighting, atmosphere and the UI automatically.
  - **Texturing seam (reserved)**: `material/LayerMaterialFactory` already reads `physical.planet.layerTextures` / `layerNormalMaps` (null today) so skinning the visible layer faces later is a data-only change.
  - **Resource-mode click glide**: clicking a hovered cliff cell should update the active traversal targets as well as the resource selection so the existing focus easing glides the camera to the clicked cell. For the lon/lat topology this means updating `focus.lonTarget` / `focus.latTarget`; for the hexsphere it means targeting `focus.dirTarget` / `focus.nCutTarget` and then letting `FocusController` do the glide.
  - **HUD overflow**: the explorer HUD is intentionally scrollable internally once it grows taller than the viewport; the scrollbar stays hidden by default and only appears as a subtle thin bar on hover / focus.
  - **Swappable cell topology**: the way the body is divided into cells lives behind the `topology/` abstraction (`topology/CellTopology.js`), selected by ONE config flag `physical.planet.cellTopology` via `topology/createTopology.js`:
    - `'lonlat'` (default) — the longitude/latitude quad grid + polar-cap workaround, wrapped in `topology/LonLatTopology.js` (owns `PolarCapModel`, `LatitudeStops`, `world/CellGrid`, `world/GridLines`, `picking/SurfacePicker`, plus the `topology/lonlat/` cut/broad-phase/traversal strategies). Behaviour unchanged from before the refactor. `behaviour.traversal.resourceTraverseAxis` is meaningful only here.
    - `'hexsphere'` — a Goldberg polyhedron (mostly hexagons + exactly 12 pentagons, **no poles**, so the pole workaround does not exist), in `topology/GoldbergTopology.js` + `topology/goldberg/` (`GoldbergSphere` subdivides an icosahedron → dual; `GoldbergGrid` extrudes per-depth prisms; `CentroidIndex`/`GoldbergSurfacePicker` do nearest-centroid picking; `GoldbergTraversal` does screen-relative drag-stroll + snap and along-wall/cut-advance adjacency arrow-stepping; `GoldbergCutStrategy` is a generic through-centre meridian cut; `GoldbergBroadPhase` is a bounding-sphere test). Frequency knob: `physical.planet.hexFrequency` (surface has 10·f²+2 cells).
    - Key enabler: every crust/atmosphere cell (quad, pentagon, hexagon) is a uniform **N-gon prism** record `{ kind, depth, lon, lat, outerRing[N], innerRing[N], corners = outer++inner, … }`, so `geometry/CellGeometryFactory`, `geometry/CrossSectionFactory`, `picking/HighlightManager`, `navigation/CrustCamera` and `hud/HudView` are topology-agnostic. The lon/lat polar-cap dome (`kind === 'cap'`) is the only special-cased primitive and lives entirely inside the lonlat topology. The focus point stays `{lon,lat}` for both topologies.
    - **Resource-mode slicing is topology-owned** via `topology.createSliceBuilder(ctx)` (SliceBuilder contract: `build(slab)`, `capMeshes[]`, `clearCaps()`, `enter()`, `exit()`) and `topology.createResourceHighlight(deps)` (`build(cell) → BufferGeometry`). `ClipController.updateCut()` just re-orients/places the plane and calls `sliceBuilder.build()`; `ModeTransition`/`BodyExplorer` call `enter()/exit()` (they no longer touch `applyClipping` directly). `ctx = { scene, clip, cellGrid, crossSection, materials, layerModel, focus, geometryFactory, bodyMesh }`; `deps = { crossSection, geometryFactory, clip }`.
      - `'lonlat'` → `slicing/CapBuilder` (GPU plane-clips the shared body materials via `BodyMesh.applyClipping` in `enter/exit`, and closes each sliced cell with a flat cross-section cap) + `topology/lonlat/LonLatResourceHighlight` (a sliced cross-section sliver + the cap-dome outer skin; the old `HighlightManager.buildResourceHighlightGeometry` moved here). Pixel-identical to before.
      - `'hexsphere'` → `topology/goldberg/CellSliceBuilder` renders **whole cells** (no half cells): a cell is kept iff its centroid is on the kept `+normal` half of the cut (`n·centroid + constant ≥ -eps`), so the **whole near hemisphere** (full surface + full-depth crust) is kept and the cliff wall AND the surface rim are a staircase of complete hex/pentagon cells (the full surface stays visible from the resource camera). Kept cells are merged into **lit** meshes reusing the FrontSide `depthMaterials` (coincident interior walls cull to one face → no z-fighting; the boundary cells' cut-facing side faces point at the camera and read as the solid cliff), each with a `faceToCell` table so `CliffPicker` is unchanged. The full core sphere is clipped to the kept half (core material `clippingPlanes`); its cut face is filled by a **solid full disc** (`#emitCoreDisc`, `coreCapMaterial`, radius `√(coreRadius²−k²)`). The crust is a solid stack of whole cells over the kept hemisphere, so NO layered cross-section backing is needed (a flat layered backing was tried in an earlier iteration but made the cells read as "cut up", so it was removed). Whole-cell highlight is `topology/goldberg/WholeCellResourceHighlight`. `BodyMesh.hideAll()/restoreView()` toggle the base meshes for hex enter/exit.
      - **Hexsphere navigation is pole-free** (`topology/goldberg/GoldbergTraversal`): the focus is a small great-circle **frame** on `state.focus` — `dir` (focus direction), `nCut` (cut-plane normal, kept ⟂ `dir` so the focus always sits ON the wall), plus eased targets `dirTarget`/`nCutTarget`. Motion is pure quaternion rotation of these unit vectors, so travel is 360° in every direction with **no pole, no clamp, no special behaviour anywhere**. **Left-drag mirrors the arrow keys** — horizontal drag / Left-Right arrows PAN along the wall (rotate `dir` about the fixed `nCut` → plane unchanged → no rebuild), and vertical drag / Up-Down arrows ADVANCE the cut (rotate BOTH `dir` and `nCut` about the wall tangent `nCut×dir` → rebuild). **Right-drag ROLLs the view** about the focus radial `dir` (`#rollTargets` rotates ONLY `nCut` about `dir`, keeping `dir`/radial-up/look-at fixed while the camera — offset along `−nCut` — orbits the focus→centre axis; mouse-left rolls left). `InputController` tracks `e.button` (and `preventDefault`s the resource-mode context menu) and passes it as the 5th `onDrag(focus,dx,dy,dpp,button)` arg (button 2 → roll, else dx→pan + dy→advance). `FocusController.easeFocusToTarget` delegates to `traversal.advance(focus,snapEase)` **when that method exists** (hexsphere slerps its frame, re-orthogonalises `nCut`, writes derived `focus.lon/lat` for the generic camera/HUD, and reports `cutChanged` — a pan reports `false` → no rebuild); when absent it keeps the lon/lat easing, so **lon/lat topology is completely untouched** (SOLID). `GoldbergCutStrategy.orient` sets `plane.normal = focus.nCut` (meridian-from-lon fallback only before the frame is seeded). `CrustCamera` is unchanged: `radialUp` reconstructed from the synced `focus.lon/lat` equals `dir` and stays continuous across the former poles, so the camera glides straight over them.
      - **Batch reveal fade** (`CellSliceBuilder`, `behaviour.slice.cellFadeMs`): cells that change membership in one `build()` call form a fade batch (`{ t0, entries: [{ dir, material, mesh }], addedKeys }`) rather than each cell animating on its own independent clock — the earlier per-cell approach (custom `alphaHash` shader + per-vertex `aT0`/`aDir` attributes rebuilt on every throttled advance) caused a very significant FPS drop with `cellFadeMs` enabled. **Multiple batches can be in flight concurrently** — with the drag-rebuild throttle (~55ms) shorter than a typical `fadeDur` (hundreds of ms), a continuous advance starts a new batch well before the previous one finishes, so forcing the old batch to complete on every new build made cells pop instead of fade; batches are now independent and don't cut each other short. `#fadingInKeys` (a `Set` of `${depth}:${cellIndex}`) tracks every cell currently fading in across ALL active batches, so the opaque set always excludes them — a cell only folds into the opaque buckets once ITS OWN batch finishes, not just because a later rebuild happened to include it (that distinction matters: without it, a cell would appear opaque after the NEXT throttled rebuild, ~55ms later, regardless of `fadeDur`). Each batch owns its own uncached cloned materials (`transparent: true, depthWrite: true`, no custom shader/derivatives) so concurrent batches touching the same depth never fight over one shared opacity value; `.opacity` is written directly each frame from a per-batch progress value (`tick(dt)` → `p = clamp((clock-t0)/fadeDur, 0, 1)`), with materials/meshes disposed once a batch's progress reaches 1. `depthWrite` stays on (like the old alphaHash approach) so overlapping staircase cells still resolve without draw-order flicker. During the transition sweep (`slab=true`) or when `cellFadeMs≈0` it hard-rebuilds with no fade (`#retireAllBatches()`).
      - Perf — **incremental, bucketed slice** (`CellSliceBuilder`): geometry rebuild is keyed on a **membership signature** (count + per-depth hash of included `cellIndex`), so a pan along the wall (same set) does zero geometry work. The opaque slice is split into coarse lon/lat SECTOR buckets (`#bucketKey`, AZ=8×EL=4 → one mesh per `(depth,bucket)` with a tight bounding sphere) held in a **persistent** `#opaqueBuckets` map; `#syncOpaque` diffs per-bucket sub-signatures and disposes+rebuilds **only the 1–2 buckets whose cell set changed** (unchanged buckets are never re-uploaded), so a continuous advance-with-fade stays cheap AND off-screen buckets are frustum-culled. Only the small transient meshes (fade meshes in `#fadeMeshList`, the core disc in `#staticTransient`, and the lazily-rebuilt atmosphere pick shell `#atmosphereMesh`) are rebuilt on change; `#refreshCapMeshes` rebuilds the pick list from the current meshes each change so raycasting never sees a disposed mesh (the core disc has no `faceToCell` → non-pickable). `SceneContext` clamps `setPixelRatio` to `min(DPR, 1.5)` (the atmosphere raymarch is per device pixel). The whole near hemisphere is kept (no far-side/strip cull — that changed the visible surface and was reverted). If a high `hexFrequency` janks, lower it; if the full hemisphere needs more perf, add a **surface-preserving** far-back cull (never a strip cull).
        - **Column-unit inclusion** (`#collect`, the big advance-FPS + correctness fix): membership is decided per **COLUMN** (one surface face = one `cellIndex`, whose depth stack lives at `cellsByDepth[0..maxDepth-1][sameArrayIndex]` — parallel arrays), NOT per cell. Each column is tested ONCE from its depth-0 surface centroid `s0 = n·c + k`: `s0 < -eps` drops the whole column; otherwise depth 0 is always emitted (surface skin kept across the whole hemisphere), and iff `s0 <= #wallBandDist` the ENTIRE deep stack (depths 1..maxDepth-1) is emitted too. WHY: the old per-cell test `s_d = n·centroid_d + k <= #wallBandDist` used each cell's OWN centroid, and a deeper cell has a SMALLER radius → SMALLER `s_d`, so at intermediate lateral distance it kept depth 0 (always) + deep cells (small s) but DROPPED depth 1–2 (their s exceeds the band) → **a hole just under the surface** ("top cells of a column not rendered"). Column inclusion guarantees a rendered cliff column is a contiguous, gap-free stack AND makes membership churn spatially coherent (the set now flips only at the moving great-circle boundary + the wall-band edge), which cut the buckets-rebuilt-per-step (and the `geom` time) sharply. The wall is now a uniform ~`wallBandCells`-column-thick slab (the old cone kept extra deep cells that were occluded anyway). `#included` is now used ONLY by the atmosphere path (atmosphere cells are depth 0 → kept across the hemisphere). Verify with `behaviour.debug.profileSlice`.
        - **Merged slice meshes are built from CACHED per-cell typed arrays** (`#mergedMesh` + `CellGeometryFactory.cellArrays`): each cell's positions/normals/index arrays depend only on its static corner rings, so they are computed once and cached on the cell (`cell.geoCache`, typed arrays). `#mergedMesh` (shared by opaque buckets, fade batches, atmosphere shell) sums the cached lengths, allocates ONE `Float32Array` per attribute + one `Uint32Array` index, and `.set()`s each cell's cache at its offset (indices offset by the running vertex base) — no intermediate JS arrays and no second copy inside a `Float32BufferAttribute`. This removed the per-corner `Vector3` clone/normalize/cross recompute AND the double array copy from the hot ~18/sec rebuild path. `#collect`'s membership test uses the separately-cached `cell.sliceCentroid` so it's cached dot-products, not geometry. (`profileSlice` in `behaviour.debug` logs a rolling avg of `build()` total vs `#collect` vs `#syncOpaque`-incl-buckets-rebuilt to confirm.)
      - **Selectable atmosphere shell**: `CellSliceBuilder` renders atmosphere slice cells with an invisible-but-pickable material (`colorWrite:false, depthWrite:false`) kept in `capMeshes`, so there is NO persistent blue box — only the `HighlightManager` hover wireframe appears (as before) while picking still resolves the cell. Because it draws nothing and is only used for picking (which is skipped while the view moves), the shell is built **lazily**: `build()` just records the cut + flags `#atmosphereDirty`; `ensureAtmosphere()` (called by `CliffPicker.pick()` before it raycasts) regenerates the whole-hemisphere shell at most once, after motion settles. Regenerating it every advance step (the old `#rebuildStaticTransient`) was a large invisible cost and dipped FPS while advancing the cut; only the VISIBLE core cut-face disc is still rebuilt every step (`#rebuildCoreDisc`). NOTE: `#collect` no longer scans atmosphere cells (that scan moved into `ensureAtmosphere`), and all membership/bucket keys are now **allocation-free numbers** (`depth * #cellStride + cellIndex`, buckets `depth * 1024 + #bucketKey`) instead of `${d}:${cellIndex}` template strings — the per-step string churn was heavy GC pressure at ~18 rebuilds/sec. `CellSliceBuilder` renders atmosphere slice cells with an invisible-but-pickable material (`colorWrite:false, depthWrite:false`) kept in `capMeshes`, so there is NO persistent blue box — only the `HighlightManager` hover wireframe appears (as before) while picking still resolves the cell. Because it draws nothing and is only used for picking (which is skipped while the view moves), the shell is built **lazily**: `build()` just records the cut + flags `#atmosphereDirty`; `ensureAtmosphere()` (called by `CliffPicker.pick()` before it raycasts) regenerates the whole-hemisphere shell at most once, after motion settles. Regenerating it every advance step (the old `#rebuildStaticTransient`) was a large invisible cost and dipped FPS while advancing the cut; only the VISIBLE core cut-face disc is still rebuilt every step (`#rebuildCoreDisc`). NOTE: `#collect` no longer scans atmosphere cells (that scan moved into `ensureAtmosphere`), and all membership/bucket keys are now **allocation-free numbers** (`depth * #cellStride + cellIndex`, buckets `depth * 1024 + #bucketKey`) instead of `${d}:${cellIndex}` template strings — the per-step string churn was heavy GC pressure at ~18 rebuilds/sec.
      - **BVH-accelerated resource-mode picking** (`three-mesh-bvh@0.7.8`, importmap in `templates/index/index.html.twig` from the same unpkg CDN as `three@0.160.0`; installed once via `core/BvhSetup.js` → `installBvh()` in `public/explorer/index.js`, which patches `BufferGeometry.computeBoundsTree/disposeBoundsTree` + `Mesh.prototype.raycast = acceleratedRaycast`). WHY: `HoverController` re-picks every pointer-move frame, and `CliffPicker` raycasts `sliceBuilder.capMeshes` = the WHOLE near hemisphere of cliff cells (depth-0 kept everywhere) PLUS the whole-hemisphere atmosphere pick shell — ~200k triangles brute-forced per frame, dropping 144→90 fps over empty space and →80 over cells the moment the cursor moved (idle stayed 144 via the cache). Fix: the cap pick meshes plus the `bodyMesh.core` sphere and the displaced-body surface mesh get a `computeBoundsTree({ indirect: true })`, so those raycasts become O(log n). `acceleratedRaycast` falls back to the stock path for any mesh WITHOUT a `boundsTree`, so all other raycasts are unchanged. Two non-obvious requirements:
        - **`indirect: true` is REQUIRED**: the default build reorders the geometry index in place and reports the reordered `faceIndex`, which breaks the `faceToCell[hit.faceIndex]` mapping (picks a consistently-offset/wrong cell); indirect mode leaves the index untouched and maps `faceIndex` back to the original triangle order via `resolveTriangleIndex`.
        - **The cap-mesh BVH is built LAZILY at pick time** (`CliffPicker#ensureBoundsTrees`), NOT eagerly in `CellSliceBuilder.#mesh()`. Picking is SKIPPED while the view moves (`HoverController` bails on `state.resourceMoving`), so building a tree for every changed bucket + the whole-hemisphere atmosphere shell on every throttled rebuild during a cut-advance was pure waste and TANKED FPS while advancing. Lazy building defers the (per-mesh, one-time) cost to the first pick after motion settles; persistent bucket meshes keep their tree across pans so only freshly rebuilt meshes pay. The core sphere / displaced surface mesh persist, so those two build once eagerly in their picker constructors. The tree is owned by its geometry and dropped on `geometry.dispose()`/GC (no explicit teardown). NOTE: the same-URL `three` import inside `three-mesh-bvh` dedupes to the single THREE instance — do NOT add a second `three` importmap entry.
        - **Per-cell geometry is cached** (`CellGeometryFactory.cellArrays`/`appendCellArrays`): a cell's raw positions/normals/index arrays depend only on its (static) corner rings, so they are computed ONCE (via `appendCell`) and cached on the cell as typed arrays (`cell.geoCache`). The merged-slice rebuild path (`CellSliceBuilder.#buildBucketMesh`/`#emitBatchMesh`/`#emitAtmosphere`, all via the shared `#appendCellCached` helper) then just COPIES the cached numbers into the merge buffers + offsets the indices, instead of re-running per-corner `Vector3` clone/normalize/cross math for thousands of cells every ~55ms advance step. Big win on the variable bucket-rebuild cost with zero visual change. `#collect`'s membership test uses the separately-cached `cell.sliceCentroid` (see `#centroid`), so the fixed per-rebuild scan is already cached dot-products, not geometry.
        - **Opt-in slice-rebuild profiling** (`behaviour.debug.profileSlice`, `profileEvery`): when on, `CellSliceBuilder.build()` logs a rolling console average of total rebuild time vs the `#collect` membership scan vs the `#syncOpaque` geometry rebuild (incl. buckets rebuilt/step) every `profileEvery` rebuilds. Use it to confirm where an advance-time FPS dip actually is before reaching for a heavier fix. A **separable sphere `#collect`** (one `n·unitCentroid` dot per column, derive all depths as scalars) was considered but DEFERRED: centroids are cached, so its steady-state gain is only dot→scalar with the same iteration count — implement only if profiling shows `#collect` dominates.
  - **Atmosphere scattering side** (`AtmosphereShell.updateForCamera`, called per frame): the haze shell flips to `THREE.BackSide` when the camera is inside it (resource mode, camera near the surface) and `FrontSide` from orbit — a static `FrontSide` culled every face from inside, so the Rayleigh scattering disappeared in resource mode. Globe-mode look is unchanged.
  - **Atmosphere scattering is throttled (cached at `physical.atmosphere.updateHz`, default 30)**: the Rayleigh+Mie raymarch is a per-pixel fragment shader that used to re-run every rendered frame (dominant fill cost). `AtmosphereShell` now keeps the haze mesh in its OWN private `THREE.Scene` (no longer added to `bodyMesh`) and renders it into a `WebGLRenderTarget` (`renderPass()`) only when `1000/updateHz` ms have elapsed (throttled in `BodyExplorer.#animate`), then re-composites that cached texture over the frame EVERY frame via a fullscreen additive raw-passthrough quad (`composite()`, called right after `scene.render()`). `updateHz: 0` = recompute every frame. The cache is view-dependent so it lags slightly during camera motion (accepted). Pixel-identical in globe mode: the RT is `NoColorSpace` + a raw (no colour-conversion) composite shader + `samples:4` MSAA, and the shader already clips the ray at the planet surface so planet occlusion needs no depth buffer. The haze is only visible in globe/`view` mode, so there's no resource-mode depth interaction to worry about. `BodyExplorer.#resize()` forwards canvas resizes to `AtmosphereShell.resize(renderer)`.
`templates/Game/game.html.twig`: Present but currently a stub; not used by the active game route.

  - The explorer is hexsphere-only now: `topology/GoldbergTopology.js` is the only cell topology wired in `public/explorer`, and the lon/lat implementation was removed.
  - **Irregular (asteroid) bodies**: the body no longer has to be a perfect sphere. Shape is a **deterministic radial displacement** owned by `model/ShapeField.js`, configured by `physical.planet.shape` (`{ type:'sphere'|'noise', seed, octaves, baseFrequency, lacunarity, gain, amplitude, maxDisplacement, axisScale }`). `type:'sphere'` returns scale 1 everywhere → pixel-identical to the original body; `type:'noise'` is a seeded (mulberry32 PRNG + 3D Perlin fBm sampled on the unit direction — no `Math.random`, no lon/lat seam/pole) heightfield `R(dir)=radius·(1+fBm)` clamped by `maxDisplacement`. **Same `radius`+`seed` ⇒ the exact same body, every reload** (will come from the backend later). Only **star-shaped** (radially single-valued) bodies are supported — true concave shapes (overhangs/tunnels/bilobed) are explicitly out of scope (they'd break the through-centre cut, centroid picking and the concentric layer model, needing a mesh/voxel topology).
    - Implementation is isolated: `GoldbergGrid.#prismCell` evaluates each corner's radius **per-direction** (shared corners are the same `Vector3` instance so radii match exactly → no cracks); the crust column is scaled between the **fixed core sphere** and the displaced surface using `LayerModel.layerFrac` (frac 1=surface … 0=core), so no layer ever inverts. `GoldbergGrid` clamps the surface to `coreRadius + 0.2·nominalCrust` as a final anti-inversion guard. Everything else (cross-section, cut plane, traversal, broad-phase, highlight, HUD) was already shape-agnostic (operates on `cell.corners`).
    - View-mode picking: `GoldbergSurfacePicker` keeps the fast analytic ray-vs-sphere ONLY for `type:'sphere'`; for displaced bodies it raycasts the merged depth-0 surface mesh (passed in via `createSurfacePicker(surfaceMesh)`) and maps the hit triangle back through `faceToCell`. `CentroidIndex` is unchanged. `CrustCamera` frames the cliff using the **local** surface radius at the focus direction (`shapeField.surfaceRadius(focus.dir)`), not the global radius. `GoldbergGridLines` lifts each corner to its displaced surface radius.
    - The **atmosphere is a perfect analytic sphere shell**, so it is turned OFF (`physical.atmosphere.show:false`) for the displaced body (its surface would poke through the haze; asteroids have no atmosphere anyway). Re-enable it together with a `shape.type:'sphere'` planet. The original perfect-sphere `planet` block is preserved (commented out) in `config/physical.js`.

---

## Instructions
- For the backend, strictly adhere to the SOLID principles. Use the Symfony features as much as possible. Always use methods that correspond to the projects Symfony version.
- There is a pattern laid out in `src/CliAccess` that should be used to call controller commands on CLI without invoking the web app for ease.
- Do not remove commented out lines when rewriting a piece of code.
- For PHP always use Allman style (opening braces on new line)
- For Typescript always use typical K&R style.
- Do not run style checks or build for TypeScript after code changes.

---

## Development workflow & environment

- **Run everything through the app container**: `docker exec angara-app php /var/www/html/bin/console <cmd>`. The app root inside the container is `/var/www/html`. Omit `-t` when calling from automated/non-interactive contexts (schema warnings print to stderr and look red in PowerShell, but the commands still succeed).
- **File sync gotcha (important):** the repo is bind-mounted (`.:/var/www/html`), but host↔container syncing is only reliable for **edits to existing files** and for files under subdirectories like `src/` and `config/`. **Newly created files at the repository root do NOT reliably sync host→container** (e.g. a freshly created `phpunit.dist.xml`). When a new root-level file must exist in the container, push it explicitly with `docker cp <hostfile> angara-app:/var/www/html/<file>`. Deletions of entity files also may not propagate — if a removed entity still shows up in `schema:update`, delete it in the container too.
- **No migrations** (project rule): apply schema with `doctrine:schema:update --force`. For a clean rebuild of a database use `doctrine:schema:drop --force --full-database` then `doctrine:schema:create`.
- **PowerShell SQL quoting:** prefer single-quoted SQL passed to `dbal:run-sql`, doubling inner single quotes for string literals, e.g. `dbal:run-sql 'SELECT ... WHERE m.identifier=''iron'''`. The MySQL reserved word table `system` must be backtick-quoted (`` `system` ``).
- Seed/reset the dev database with `make fixtures` (or `make database` for a full reset). The world id changes on every fixtures reload, so re-query `SELECT id FROM world WHERE identifier='angara'` rather than hard-coding it.

## Testing

- Tooling: **PHPUnit** (root `phpunit.dist.xml`, bootstrap `tests/bootstrap.php`). Tests run in the `test` environment against a **dedicated `<dbname>_test` database** (configured via `dbname_suffix` in `config/packages/doctrine.yaml` under `when@test`), so they never touch dev data.
- **Run the suite with `make test`.** It (re)creates the test DB schema from the current entity metadata (no migrations) and runs PHPUnit. Because the schema is rebuilt from metadata each run, entity changes are picked up automatically.
- On Windows PowerShell hosts where `make` is not installed, run PHPUnit directly in the app container: `docker exec angara-app php /var/www/html/bin/phpunit -c /var/www/html/phpunit.dist.xml`.
- Tests live **per bundle** under `src/<Bundle>/tests/`, namespace `App\<Bundle>\Tests\` (registered in the root `composer.json` `autoload-dev`). The economy coverage is `src/GameCoreBundle/tests/Functional/Economy/EconomyTickTest.php`, with test-only fixtures under `src/GameCoreBundle/tests/Fixtures/`.
- Functional tests boot the kernel (`KernelTestCase`) and load fixtures programmatically via Doctrine's `ORMExecutor`/`ORMPurger`. A test may build a large dataset cheaply with bulk DBAL inserts in a dedicated fixture (see `ScaleEconomyWorldFixture`) instead of hydrating thousands of entities.
- When adding a test that needs a specific dataset, add a **dedicated fixture for that test** rather than reusing/altering the demo seed.
- There is a `create-unit-test` skill under `.agents/skills/` for *unit* tests; follow it when asked for unit tests (it does not apply to functional tests).

## Economy tick architecture (scalability)

The economy advances in ticks (`app:economy:tick`, orchestrated by `EconomyTicker`). It is built to scale to hundreds of systems × hundreds of bodies, so keep these invariants when working on it:

- **ORM-free and set-based.** The tick never hydrates the world graph. Input is read via `WorldEconomyReader.streamResourceRows()`, which streams **one system at a time** (each system is a small buffered query, freed before the next) to keep memory flat while preserving system-contiguous order.
- **Streaming two-pass, no O(N) in memory.** Pass 1 (`TickCalculator.aggregate`) folds the row stream into small bounded aggregates and pushes per-body states/flows to writer sinks; pass 2 (`StateEvolver.streamChanges`) re-streams and writes back next-tick reserves/stock. The `TickReport` only retains the bounded aggregates; per-body detail is collected **only** when `EconomyTicker::tick($world, collectDetail: true)` (used for rendering small worlds, never at scale).
- **Bounded current-state read model.** Per-tick results are written to `current_*` tables (one row per body/system/material, overwritten each tick) via bulk DBAL inside a single `connection->transactional()`. No append-only history.
- Bodies can be owned by a player: `CelestialBody.owner` (`player_id`, nullable) ↔ `Player.celestialBodies`.

---

## Code style:

#### Classes
- Avoid getters when possible and use the public private(set) or equivalent.

#### Anonymous functions
- Always use arrow functions, when possible and readable
- Always add a return type
- Always use `static` when the function can be
- Always add a space between `fn` and `(`
- The format should be `static fn (): ReturnType => ...`.

#### Comments
- Method names should not be commented, it should be self-explanatory.

#### Blocks
- Block statements must have a newline above and below (if not the first or last line in another block).

#### Trailing comma
- Always use trailing commas in multi-line arrays or method arguments when each element is on a separate line.
